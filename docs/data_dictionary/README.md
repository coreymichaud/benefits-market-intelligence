# Structure

This `data_dictionary/` folder is structured by medallion layer (bronze/silver/gold). Within each folder is a markdown file with the name of the table from `data/warehouse/form_5500.db` with information about it.